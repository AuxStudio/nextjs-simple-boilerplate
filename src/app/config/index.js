import routes from './routes';
import SEO from './SEO';
import styleConstants from './styleConstants';
import version from './version';

const config = {
  routes,
  SEO,
  styleConstants,
  version,
};

export { routes, SEO, styleConstants, version };

export default config;
