import { rhythm } from '../../static/styles/styleConstants';

const styles = {
  container: {
    borderRadius: 0,
    textTransform: 'initial',
    fontWeight: '700',
  },

  spacer: {
    width: rhythm.hz / 2,
  },
};

export default styles;
