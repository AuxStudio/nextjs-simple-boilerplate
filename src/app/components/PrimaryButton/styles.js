import { rhythm } from '../../static/styles/styleConstants';

const styles = {
  container: {
    textTransform: 'initial',
    fontWeight: '700',
    height: 42,
  },

  spacer: {
    width: rhythm.hz / 2,
  },
};

export default styles;
