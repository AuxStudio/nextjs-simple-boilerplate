import css from 'styled-jsx/css';

const styles = css`
  .cancel-button-container {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    display: flex;
    align-items: center;
  }

  .logo-image {
    width: 64px;
    cursor: pointer;
  }
`;

export default styles;
