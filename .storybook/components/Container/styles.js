import { sizes } from '../../../src/app/static/styles/styleConstants';

const styles = {
  container: {
    maxWidth: sizes.maxContentWidth,
    margin: '0 auto',
    display: 'flex',
    justifyContent: 'center',
  },
};

export default styles;
